# Native plugins

This repository is the first-party catalogue for Native plugins and workflow packs.

The packages here are thin distribution layers. They provide product metadata, skills,
and connections to independently deployed Native services; they do not bundle service
implementations, databases, credentials, or workspace data.

The first package, `native`, is being prepared for review.
